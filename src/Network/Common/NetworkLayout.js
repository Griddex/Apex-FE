import React from "react";
import PropTypes from "prop-types";

const NetworkLayout = (props) => {
  return (
    <div>
      <h1>Network Layout</h1>
    </div>
  );
};

NetworkLayout.propTypes = {};

export default NetworkLayout;
